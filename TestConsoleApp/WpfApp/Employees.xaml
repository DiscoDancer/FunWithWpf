<Page x:Class="WpfApp.Employees"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      Title="Employees">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="Add" Width="500" x:Name="AddBtn"/>
        <Grid Grid.Row="1" Width="500">
            <DataGrid x:Name="EmployeeDataGrid" IsReadOnly="True" ItemsSource="{Binding}">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding EmployeeID}" Header="Employee ID"/>
                    <DataGridTextColumn Binding="{Binding LastName}"  Header="Last Name"/>
                    <DataGridTextColumn Binding="{Binding FirstName}"  Header="First Name"/>
                    <DataGridTextColumn Binding="{Binding MiddleName}"  Header="Middle Name"/>
                    <DataGridTextColumn Binding="{Binding Position}"  Header="Position"/>
                    <DataGridTextColumn Binding="{Binding Salary}"  Header="Salary"/>
                    <DataGridTextColumn Binding="{Binding Phone}" Header="Phone"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Page>
