<Page x:Class="WpfApp.Orders"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300"
      Title="Orders">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Height="50" VerticalAlignment="Top">
            <Button VerticalAlignment="Center" MinWidth="100" Margin="400,0,0,0">
                <Hyperlink NavigateUri="Customers.xaml" FontSize="14" FontWeight="Bold" FontFamily="Times New Roman" Foreground="Black" TextDecorations="none">
                    Customers
                </Hyperlink>
            </Button>
            <Button  VerticalAlignment="Center" MinWidth="100" Margin="10,0,0,0">
                <Hyperlink NavigateUri="Employees.xaml" FontSize="14" FontWeight="Bold" FontFamily="Times New Roman" Foreground="Black" TextDecorations="none">
                    Employees
                </Hyperlink>
            </Button>
            <Button  VerticalAlignment="Center" MinWidth="100" Margin="10,0,0,0">
                <Hyperlink NavigateUri="Products.xaml" FontSize="14" FontWeight="Bold" FontFamily="Times New Roman" Foreground="Black" TextDecorations="none">
                    Products
                </Hyperlink>
            </Button>
            <Button VerticalAlignment="Center" MinWidth="100" Margin="10,0,0,0" IsEnabled="False">
                <Hyperlink NavigateUri="Orders.xaml" FontSize="14" FontWeight="Bold" FontFamily="Times New Roman" Foreground="Black" TextDecorations="none">
                    Orders
                </Hyperlink>
            </Button>
        </StackPanel>
        
        <Button Grid.Row="0" Content="Add order" Background="LightGreen" BorderThickness="2" Width="70" HorizontalAlignment="Left" VerticalAlignment="Bottom" x:Name="AddBtn"/>
        <Grid Grid.Row="1">
            <DataGrid x:Name="OrderDataGrid" IsReadOnly="True" VerticalAlignment="Top" AutoGenerateColumns="False" ItemsSource="{Binding OrderDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding OrderID}" Width="*" Header="Order ID"/>
                    <DataGridTextColumn Binding="{Binding CustomerID}" Width="*" Header="Customer ID"/>
                    <DataGridTextColumn Binding="{Binding EmployeeID}" Width="*" Header="Employee ID"/>
                    <DataGridTextColumn Binding="{Binding ProductID}" Width="*" Header="Product ID"/>
                    <DataGridTextColumn Binding="{Binding Quantity}" Width="*" Header="Quantity"/>
                    <DataGridTextColumn Binding="{Binding Price}" Width="*" Header="Price"/>
                    <DataGridTextColumn Binding="{Binding OrderDate}" Width="*" Header="Order Date"/>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Button Content="Edit" Background="Orange" Width="35" Tag="{Binding Path=id_Edit_Order}"></Button>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Button Content="Delete" Background="Red" Tag="{Binding Path=id_Del_Order}"></Button>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Page>
